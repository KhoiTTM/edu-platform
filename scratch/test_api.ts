import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; import path from 'path'; const envPath = path.resolve(process.cwd(), '.env.local'); const envContent = fs.readFileSync(envPath, 'utf-8'); const env = {}; envContent.split('\n').forEach(line => { const parts = line.split('='); if(parts.length>=2) env[parts[0].trim()] = parts.slice(1).join('=').trim().replace(/^['"]|['"]$/g, ''); }); const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data: source } = await supabase.from('content_sources').select('id').eq('slug', 'tieng-anh-3-global-success').single(); const { data: nodes } = await supabase.from('curriculum_nodes').select('id').eq('source_id', source.id).eq('type', 'lesson').limit(1); const conceptIds = [nodes[0].id]; console.log('Testing conceptIds:', conceptIds); const req = await fetch('http://localhost:3000/api/assessment/generate', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ subjectSlug: 'tieng_anh_3', conceptIds: conceptIds, difficulty: 'medium', count: 15 }) }); const res = await req.json(); console.log('Response:', JSON.stringify(res).slice(0, 500)); } run();