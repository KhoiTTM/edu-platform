import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; import path from 'path'; const envPath = path.resolve(process.cwd(), '.env.local'); const envContent = fs.readFileSync(envPath, 'utf-8'); const env = {}; envContent.split('\n').forEach(line => { const parts = line.split('='); if(parts.length>=2) env[parts[0].trim()] = parts.slice(1).join('=').trim().replace(/^['"]|['"]$/g, ''); }); const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data } = await supabase.from('assessment_collections').select('id').ilike('title', 'Tiếng Anh 3%').like('reference_book', '%Toán%'); if(data?.length) { const ids = data.map(d=>d.id); console.log('Deleting ' + ids.length + ' collections...'); const res = await supabase.from('assessment_collections').delete().in('id', ids); console.log('Delete result:', res.error || 'Success'); } } run();