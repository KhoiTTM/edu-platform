import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; import path from 'path'; const envPath = path.resolve(process.cwd(), '.env.local'); const envContent = fs.readFileSync(envPath, 'utf-8'); const env = {}; envContent.split('\n').forEach(line => { const parts = line.split('='); if(parts.length>=2) env[parts[0].trim()] = parts.slice(1).join('=').trim().replace(/^['"]|['"]$/g, ''); }); const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data: source } = await supabase.from('content_sources').select('id').eq('slug', 'khtn-7-ket-noi').single(); const { data: nodes } = await supabase.from('curriculum_nodes').select('id, metadata').eq('source_id', source.id).eq('type', 'lesson'); for (const node of nodes) { const meta = node.metadata || {}; meta.drive_file_id = '12HmPnGbQ5qmtOUy9ut1KbMDfR5DqBQzf'; await supabase.from('curriculum_nodes').update({ metadata: meta }).eq('id', node.id); console.log('Updated node ' + node.id); } } run();