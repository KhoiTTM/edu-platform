import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; import path from 'path'; const envPath = path.resolve(process.cwd(), '.env.local'); const envContent = fs.readFileSync(envPath, 'utf-8'); const env = {}; envContent.split('\n').forEach(line => { const parts = line.split('='); if(parts.length>=2) env[parts[0].trim()] = parts.slice(1).join('=').trim().replace(/^['"]|['"]$/g, ''); }); const supabase = createClient(env.NEXT_PUBLIC_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data } = await supabase.from('exercise_sets').select('id, title, metadata'); console.log('Total Exercise Sets:', data?.length); const engSets = data?.filter(es => es.title?.toLowerCase().includes('tiếng anh')) || []; console.log('Tieng Anh Exercise Sets:', engSets.length); const mathSets = data?.filter(es => es.title?.toLowerCase().includes('toán')) || []; console.log('Toan Exercise Sets:', mathSets.length); } run();